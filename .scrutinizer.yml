build:
  environment:
    php:
      pecl_extensions:
        - inotify
    mysql: 5.7

  tests:
    before:
      - ./bin/phing kernel
    override:
      - command: './bin/phing unit'
        coverage:
          file:   'test/coverage.xml'
          format: 'php-clover'
